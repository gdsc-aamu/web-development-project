// TODO: This component represents a single professor card.
// 1. Accepts props for professor name, department, and rating.
// 2. Uses a simple and clean layout.
// 3. Clicking the card should navigate to the professor’s profile page.

export default function Card({ professor }) {
    return (
      <div>
        {/* TODO: Display professor's name, department, and average rating */}
  
        {/* TODO: Make the entire card clickable to go to professor page */}
      </div>
    );
  }
  